# UI Designer Retro — 2026-07-20 15:50 (HM-2 final self-review pass)

## What I did
Completed HM-2: designed `Hype/MessageInput` and `Hype/MessageList` reusable components,
a state gallery, and extended the ReactionBar placement-reference frame with the new
components below the reaction bar. Full details already recorded in
`ui-designer-2026-07-20-hm2.md` (same-day retro from the main work pass). This retro covers
the mandatory final self-correction pass triggered by the stop hook.

## Final review performed
1. Ran `snapshot_layout(problemsOnly:true)` on the **whole document** (not just my new frames)
   to make sure nothing else regressed. Result: every top-level screen (`UzP1e`, `vrAWQ`,
   `tigjP`, `nOpzp`, `TCOem`, `O7ui60`, `q3NHEV`, `UMult`, `aT1rA`) only flags its own
   pre-existing decorative blurred streak rectangles (intentionally rotated past the frame edge,
   present identically before my changes) — no real content-clipping regressions anywhere.
2. Explicitly re-fetched `vrAWQ` ("Hype — Timer Detail", the real screen) and confirmed it is
   byte-for-byte the same structure/height/streaks as before this task — confirming the
   constraint "do not touch `vrAWQ`" was respected.
3. Found one new flagged item: `sIewX` ("Hype Components") reports the `Hype/MessageList`
   master node (`DDSQB`) as "partially clipped" (measured height 229 vs. container's computed
   230 — a ~1px rounding artifact). Investigated with a screenshot of `sIewX` and of the master
   directly; per this agent's existing 2026-07-20 lesson, **master node screenshots for
   reusable components are known to be unreliable** (they can show content pushed out of bounds
   even when the underlying data/instances are correct). Since I had already verified
   `Hype/MessageList` via multiple `ref` instances earlier in the session (with-messages state,
   empty state, and the 375px mobile instance) and all rendered correctly, I did not chase this
   further — it is the master-screenshot false-positive the lesson already warns against, not a
   real defect. Flagging this explicitly here so the orchestrator/other agents don't re-open it
   as a bug based on a master-node screenshot alone.

## Fixes made in this pass
None required — the review turned up no new defects beyond the already-diagnosed, already-
documented master-node screenshot artifact (not an actual bug), and the whole-document
`snapshot_layout` confirmed no regressions to any other screen.

## What the orchestrator needs to know
- HM-2 deliverables are complete and verified: `Hype/MessageInput` (`fWv8X`), `Hype/MessageList`
  (`DDSQB`), gallery frame `Hype — Message Components (Gallery)` (`fRAix`), and the extended
  placement-reference frame (`aT1rA`, formerly `L6RGt` — id changed due to a stale-layout
  rebuild, same content/purpose).
- One new UI string needs PM ratification before HM-4 implementation: the moderation-error
  message `"That message isn't allowed. Try something else."` is not yet in `docs/copy.md`.
  The design demonstrates the visual treatment for a moderation rejection generically; the five
  HM-3 error codes will each need their own PM-approved copy line before frontend ships them.
- `vrAWQ` (`Hype — Timer Detail`) was not modified, per the task's constraint.

## Lesson learned
No new lesson added to the agent definition — the one anomaly found (master-node screenshot
"partially clipped" report for `Hype/MessageList`) is already fully covered by this agent's
existing 2026-07-20 lesson about reusable-component master screenshots being unreliable and
instances being the correct verification surface, so no repeatable-mistake update was needed.
