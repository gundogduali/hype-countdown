# Retro — qa-engineer, 2026-07-20 16:13 (RX-5 self-correction pass)

## What I did (this pass)
This is the mandatory self-correction pass on top of my already-completed RX-5 work (full details in `qa-engineer-2026-07-20-rx5.md`). I re-reviewed my own report against the task definition and closed two gaps I found on critical re-read:

1. **413 `payload_too_large` and malformed-body cases (`invalid_json`/`invalid_body`) for `POST /api/timers/:slug/react`** — my original pass had read and trusted the backend developer's own tests for these (`api.test.js`), but per my own agent rule ("prove it, don't trust the developer's claim") I had not independently re-run them myself against a live instance. Fixed by spinning up one more fresh, isolated backend (`PORT=3098`, throwaway DB) and independently curling: a >100KB body → `413 payload_too_large`; truncated/malformed JSON → `400 invalid_json`; a JSON array body → `400 invalid_body`; a JSON primitive (`42`) → `400 invalid_json`; a request with no `content-type` header (so the body never gets parsed as JSON, `emoji` ends up missing) → `400 invalid_reaction_emoji`, which is correct behavior, not a bug. All matched the documented contract exactly.
2. **Process-cleanup re-verification** — re-checked `ps`/`lsof` for any of my own leftover processes after the additional verification instance above; killed PID 6279 (the `PORT=3098` instance) and re-confirmed via `ps`/`lsof` that it, and everything else I'd started earlier, is actually dead.
3. **Re-ran the backend persistent suite once more** (`cd backend && npm test`) on a clean process tree: 134/134 passing, no interference from any leftover instance.

## What I found
No new bugs. The additional independent checks confirmed the documented error shapes exactly (`413 payload_too_large`, `400 invalid_json`, `400 invalid_body`) for the reactions endpoint, matching `docs/api.md`. This closes the one real gap in my original pass: I had verified the *emoji-validation* and *dedup/race* edge cases myself from scratch, but had relied on reading (not re-running) the developer's own body-size/malformed-JSON tests for this specific endpoint.

## What I fixed
- No code was changed (not my job). I closed a verification gap by independently re-running the previously-only-read test cases myself against a live instance, and by re-confirming process cleanup with fresh `ps`/`lsof` output after the extra verification run.

## What the orchestrator needs to know
- All RX-5 findings from my main retro (`qa-engineer-2026-07-20-rx5.md`) stand: **no bugs found**, all PRD §9.2 bullets PASSED, persistent tests added (`backend/test/qa-reactions.test.js`, `frontend/e2e/reactions.mjs`, new `frontend/package.json` `test` script). This pass only adds independent confirmation of the 413/malformed-body edge cases and re-confirms clean process teardown; it does not change the overall verdict.
- No new lesson this pass — the mistake I closed (reading vs. independently re-running a developer's own tests for one specific edge-case category) is already covered by my top-level agent rule ("prove it, don't trust the developer's claim"); it isn't a new *repeatable* failure mode distinct from that existing rule, so I did not add a duplicate lessons-learned line for it.

## Not tested / limitations (unchanged from the main retro)
- Genuine cross-OS-process concurrent SQLite writes, rate-limit window expiry (real hour wait), and pixel-level design fidelity against the `.pen` file — same reasons as documented in `qa-engineer-2026-07-20-rx5.md`.
