# Retro — code-reviewer — 2026-07-09 (REV-3: English-switch combined review)

## What I did
- Reviewed BE-5 + FE-3 together for cross-cutting consistency: contract sync, copy fidelity, migration/seed correctness, residual Turkish, full verification run.
- Verified beyond the existing suite: legacy migration against a realistic legacy DB **with the old indexes present** (the unit test creates the legacy table without them) and **second-boot idempotency** (row-for-row identical snapshot, schema untouched) — both pass.
- Live combined run on my own ports (backend :3299 temp DB, Vite :5299 via `API_PROXY_TARGET`): smoke 19/19, plus a 39-assertion rendered-copy check against `docs/copy.md` on all four screens — all pass. Backend 29/29, lint+build clean.
- One direct fix: stale `docs/api.md (v1)` comment in `frontend/src/api/client.js` → v2.1 (lint+build re-verified).

## Findings for the orchestrator
- `docs/qa-report.md` is still fully Turkish (and describes v2.0 UI strings) — it was in neither BE-5 nor FE-3 scope. Needs a decision: translate, regenerate against v2.1, or archive before the public release.
- Two minor copy/deck nits flagged, not fixed (PM owns the deck): 404 button `← Back to Explore` vs deck's exact `Back to Explore`; `about {n} minutes` yields "about 1 minutes" at n=1 (same grammar in the backend 429 message).

## Lessons
- No repeatable mistake this run. Applied prior lessons: verified every claim I report empirically; used a flag-free approach to browser checks (no one-shot mocks); temp DB + own ports; killed my processes by PID and confirmed ports clear.
- Worth keeping: when a migration test hand-builds the legacy schema, rebuild it the way the *old code* actually did (indexes, pragmas) — that's where rename/drop ordering bugs hide. It didn't bite here, but the unit test's simplified legacy fixture would not have caught an index-name collision.
