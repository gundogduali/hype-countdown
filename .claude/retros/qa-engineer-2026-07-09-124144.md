# Retro — qa-engineer, 2026-07-09 12:41 (QA-1: MVP acceptance criteria)

## What I did
- Verified all 10 acceptance criteria of PRD v2.0 Section 7 by actually running the application: backend (temporary `DB_PATH`, :3001) + Vite dev (:5173), headless Chrome (playwright-core 1.61.1 installed into the scratchpad + system Chrome).
- 38 API checks (list/filter/404/13 validation scenarios/concurrent double POST/offset normalization) + 33 browser checks (live counter math ±0 s, create flow, slug URL from a clean context, reading clipboard content, +1 hour skewed client clock, live-watching the "Time's up!" boundary moment, reload, empty category state).
- Destructive tests on a separate instance (:3101, separate DB): 20×201 → 21st POST 429 + Retry-After 3600.
- Persistence: backend kill + restart with the same DB → custom timers persist, seed idempotent.
- Outputs: `docs/qa-report.md` (verdict: **READY TO SHIP**, 10/10 PASSED) + persistent e2e smoke `frontend/e2e/smoke.mjs` (19/19 PASS).

## What I found in review, what I fixed
- 2 new low-severity product findings (reported; fixes belong to their owners): (B-1) >100KB JSON body → 500 (`entity.too.large` not caught in `backend/src/app.js:63-71`), (B-2) `/favicon.ico` 404 console noise (`public/favicon.svg` not linked).
- Self-review: report ↔ raw result counts consistent; `npm run lint` clean with the e2e file I added; ports free; dev DB identical before/after testing down to the shasum (24 rows / 17 curated) — no mistake of my own requiring a fix.

## What I could not test / limits
- The rate limit's UI message (not triggered in the UI to avoid locking the dev limiter for 1 hour; verified at the API level, the mapping exists in `Create.jsx mapServerError`).
- Fidelity to Pencil: screenshot-based visual inspection, not pixel comparison (the design is user-approved + FE code-reviewed).
- The clipboard fallback branch (modern Chrome uses the primary path) and a real timezone other than GMT+3 (UTC conversions verified mathematically).

## What the orchestrator needs to know
- The MVP is ready to ship; B-1/B-2 can go into the v1.1 backlog (not blockers).
- `frontend/e2e/smoke.mjs` can be used for regressions: `node e2e/smoke.mjs` with the servers up; it SKIPs the browser steps if playwright-core is missing; each run creates 2 custom timers (a temporary DB is recommended — documented in the header).
- I made no repeatable mistake → no lesson added to the agent definition file.
