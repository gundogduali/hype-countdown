# Retro — code-reviewer — 2026-07-10 (review of DP-1: Render deploy prep)

## What I did
- Reviewed the uncommitted DP-1 diff (backend/src/app.js, backend/src/server.js,
  render.yaml, backend/test/static.test.js) in order: correctness → security →
  contract → simplicity.
- Ran the backend suite myself (39/39 green, twice).
- Parsed render.yaml with a real YAML parser (installed `yaml` in the
  scratchpad — no pyyaml/js-yaml available otherwise).
- Replicated the Render runtime shape locally: booted `node
  backend/src/server.js` from the repo root with relative
  `STATIC_DIR=frontend/dist`, `TRUST_PROXY=true`, `NODE_ENV=production`,
  port 3277, throwaway DB in the scratchpad. Probed: SPA routes, hashed-asset
  caching, favicon no-cache, API 404 JSON codes, non-GET fallback, two path
  traversal attempts (`/..%2f..%2f...` and `--path-as-is /assets/../../...`),
  spoofed-XFF POST. Killed my server by PID; :3001/:5173 untouched.
- Checked things the task called out specifically: health check
  `GET /api/timers` is NOT rate limited (limiter is POST-only) so Render's
  probes can't 429-loop; STATIC_DIR cwd-relative resolution matches Render's
  repo-root start; Express is 4.22.2 so `app.get('*')` is valid;
  NODE_VERSION=24 satisfies `engines >=22.5.0`; docs/api.md untouched.

## What I found
- No defects. Verdict: ship-ready.
- Nit (probable, non-blocking): `/assets/*` misses fall to the SPA fallback
  (200 index.html) — post-redeploy stale-hash requests get an HTML MIME error
  instead of a clean 404. Same user impact; reported, not changed.
- Future note: `app.get('*')` breaks on an Express 5 upgrade (path-to-regexp
  v8 wildcard syntax). Fine today; flagged for whoever bumps the major.

## What I fixed
- Nothing in product code (nothing unambiguous to fix).
- Team update (Rule 6): sharpened Rule 7 in
  `.claude/agents/backend-developer.md` into a bidirectional contract-test
  mapping rule — third recurrence of "test assertion written from
  memory/assumption instead of the source of truth" (`category:""` gap,
  `invalid_body`/`invalid_json` primitives assumption in be2, and today's
  `not_found` vs `timer_not_found`). Old rule triggered only on contract
  edits; both recent incidents came from writing NEW tests.

## For the orchestrator
- DP-1 is safe to commit and push as-is. Include my backend-developer.md
  Rule 7 edit in the commit (it's folded into the same modified file).
- Traversal probes returned index.html (content-type text/html), not the
  targeted files — no static-file leak.

## Lesson
- No repeatable mistake this run (hook-path, scratchpad-DB, and kill-by-PID
  lessons all applied); agent file untouched except the Rule 6 team update.
