# Retro — code-reviewer — 2026-07-07 20:05 (BE-1 Hype backend review)

## What I did
- `docs/api.md` ↔ implementation compared line by line: 3 endpoints, status codes, nested `{error:{code,message}}`, `serverNow`, `Retry-After`, `invalid_json` — all consistent.
- Seed verified record by record against PRD Appendix A: 17/17 (slug, title, emoji, category mapping, UTC dates exact). Upsert idempotent, `created_at` preserved, the `WHERE is_curated=1` guard protects custom rows.
- Tests run (22/22 → 23/23 after the fix). Live probes (with a temporary DB): SQLi (slug+category, parameterized ✓), array/object/non-object bodies, POST without content-type, 160 code-unit unicode title, year 9999, offset formats, trailing slash, repeated query param.
- Verified that the ISO string comparison (`target_at > ?`) is safe: both sides are the fixed-length `toISOString()` format → lexicographic = chronological.

## What I found, what I fixed
1. **Low — fixed:** `GET /api/timers?category=oyun&category=spor` (Express → array) silently returned the unfiltered list; the route collapsed non-strings to `undefined`, swallowing invalid input. The route now passes the value to the service as-is → `400 invalid_category`. Regression test added (`test/api.test.js`).
2. Reported (not fixed — contract decisions): no upper date bound (year 9999 accepted); the `emoji` field accepts arbitrary short text (including `<script>` — React escapes it, low risk); title length counts UTF-16 code units (stricter than "80 characters" for astral characters — errs safe); `trust proxy` unset (behind a reverse proxy the rate limit collapses to a single IP — deployment note).
3. False positive eliminated: suspected rate-limiter memory leak — the per-IP array is bounded by the limit, the map is swept above 10k; sufficient for the MVP.

## Team observation
No 2+ recurring pattern in backend-developer. "Silently normalizing an unexpected input type" (a query param can be an array) was seen for the first time — on recurrence, a rule "reject and test non-type forms (array, object) of Express query/body fields" will be added to the agent definition.

## My own lessons
I left the probe server as a background job and the command hit the timeout (results were obtained, no harm) — in live probes, kill the server definitively at the end of the same command or use run_in_background. Not a recurring mistake warranting a lesson in the definition file.
