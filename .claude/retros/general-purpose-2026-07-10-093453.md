# Retro — general-purpose — TR-2: translate .claude/ to English

## What I did
Translated all remaining Turkish content under `.claude/` to English: 6 agent definitions (bodies + frontmatter `description` values; frontmatter keys, `tools:` lists, and `model:` untouched), 23 Turkish retro files + `retros/README.md` (in place, filenames kept), `state/BOARD.md` (full board; task IDs, dates, counts, and check marks preserved), `hooks/subagent-retro.sh` (comments + the block-reason message only; logic, exit codes, JSON structure, and the agent-name case patterns untouched — `bash -n` passes), and the `statusMessage` string in `settings.json`.

## What I found in self-review
- "dev 4-birim sayaç" could have been mistranslated as "development counter" — "dev" is Turkish for "giant"; rendered as "giant 4-unit counter" in both FE retros.
- Historical Turkish UI/data literals quoted in retros (`✓ Kopyalandı`, `N AKTİF TIMER`, `iPhone 18 Tanıtımı (beklenen)`, `Gövde çok büyük (limit 100KB).`, `Hedef tarih en fazla 100 yıl ileride olabilir.`, `Hype ⏳ — Popüler Geri Sayımlar`, `OYUN`, `ÖZEL GÜNLER`, `oyun`/`spor` category values) are data, not prose — kept verbatim in code spans with English glosses so the review history stays accurate.
- Lessons Learned entries: counts and `[YYYY-MM-DD]` dates verified identical after translation (orchestrator 4, ui-designer 4, frontend 3, backend 5, qa 1, code-reviewer 3); already-English entries left byte-identical.

## What I fixed
No post-hoc fixes were needed; the literal-with-gloss convention was applied uniformly during translation.

## What the orchestrator needs to know
- One deliberate semantic deviation: orchestrator.md's "Kullanıcıyla Türkçe konuşursun" ("You speak Turkish with the user") was dropped rather than translated literally, since it contradicts the all-English directive. If you want an explicit language rule, add "You communicate with the user in English."
- The hook's block message now says 'Lessons Learned' — consistent with the translated section headings in all agent files, so the self-correction loop still lines up end to end.
- Final grep sweep over `.claude/`: zero Turkish prose; the only Turkish-character hits are the historical literals listed above (all inside code spans, all glossed).
