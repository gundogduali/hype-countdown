# Retro — qa-engineer, 2026-07-10 (QA-2: v2.1 English release verification)

## What I did
- Re-verified all 10 PRD v2.1 Section 7 acceptance criteria end to end on isolated instances (backend :3101/:3102 + Vite :5273 via `API_PROXY_TARGET`, throwaway `DB_PATH`); the user's dev servers and `backend/data/` were never touched, and the dev DB was confirmed clean afterwards.
- Leaned on `frontend/e2e/smoke.mjs` (19/19 PASS); hand-tested what it doesn't cover: ended state (live flip, no refresh), refresh correctness, +2 h skewed client clock, persistence across restart, expired-curated exclusion, empty-category UI, clipboard feedback, concurrency, UTC normalization, rate limit (429 + UI message), 413.
- English copy conformance: verbatim `docs/copy.md` assertions on all 5 screens, `lang="en"`, Turkish scans (rendered text + source), screenshots for design fidelity.
- Legacy migration: booted the current backend against a hand-built old-Turkish-schema DB — rebuild, `oyun`→`games` mapping, custom-row preservation, old-slug cleanup, 17/17 English seed. Fresh-DB path also verified.
- Regenerated `docs/qa-report.md` in English (replaced the Turkish v2.0 report). Verdict: **READY FOR RELEASE, 10/10 PASS**.

## What the self-review found (and I fixed)
- **Factual error in my own report**: I attributed the source-grep false positive on `categories.js:7` to "the emoji variation selector". Re-diagnosed with hexdump: the real cause was my grep word list containing `Spor`, which substring-matches English "Sports". Corrected the report wording — a public release document must not contain an unverified explanation.
- Earlier in-run: two false FAILs from my ad-hoc Playwright script (`isVisible({timeout})` ignores the timeout; `waitForResponse` after `click()` races). Re-verified with `waitFor()`/`Promise.all` — product was fine. Lesson recorded in my agent file.

## What the orchestrator should know
- New finding **F-1 (low, test infra)**: `frontend/e2e/smoke.mjs:95` plural-only regex vs FE-5's grammatical singular aria-label (`Countdown.jsx:30,73`) → ~7% intermittent false FAIL. One-character-class fix suggested in the report; owner: frontend/QA.
- Info for PM: a few implemented English strings are missing from the `docs/copy.md` deck (toast, create helper, `+ Create`, server-error mappings) — deck omissions, not bugs.
- A host restart mid-run killed all processes (including the user's dev servers — not my doing); evidence was already captured, I only re-launched my own instances for screenshots and cleaned them up.

## Not tested / limitations
- Design fidelity via screenshot review, not pixel-diff against the .pen file.
- Rate-limit window expiry (1 h) not waited out live — covered by unit tests with injected clocks.
