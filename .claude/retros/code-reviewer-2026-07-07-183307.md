# Retro — code-reviewer — 2026-07-07 18:33

## What I did
Full review of the Focus Garden backend (`backend/`): 5 endpoints verified line by line against `docs/api.md` (status codes, `{error, message}`, 409/422, `{completed_now}`, plant pools, ended_at semantics). Tests run (16/16 at the start), additionally green under TZ=Pacific/Kiritimati and TZ=America/Anchorage. Live probes: 2 s grace (1.5s early → 200, 2.5s early → 422), SQLi attempts (parameterized queries, table intact), weird id coercion, body-less PATCH, burst of 10 concurrent POSTs (exactly 1×201 — the single-active invariant holds).

## What I found, what I fixed
1. **Medium — fixed:** `start()` still counted an expired "active" session as active and returned 409 (`src/services/sessions.js`). PRD: "a session that expires while the app is closed counts as completed". Now, before POST, it is auto-completed with the same rule as GET /active (ended_at = planned end). Regression test added; edge cases re-verified in the self-correction round: not-yet-expired active → still 409, exact boundary (now==endMs) → 201, stats consistent.
2. **Low — fixed:** the "streak across a real day boundary" test used a real `new Date()` → flaky between 23:35–00:00 local. Converted to fixed local dates (deterministic in every TZ).
- Final state: **17/17 tests passing.**

## What the orchestrator needs to know
- Finding #1 was a gap the contract didn't define for POST; the behavior is a consistent extension of the GET /active rule. A sentence could be added to `docs/api.md` if desired (I didn't touch docs).
- Unfixed suggestions: (a) `abandon` on an expired session still produces 🥀 — a contract decision; (b) no partial unique index for single-active (defense in depth; I didn't touch it due to migration risk on the existing DB); (c) `/api/health` is not in the contract (harmless).
- No team update: no 2+ recurring mistake pattern in backend-developer. The "applying a server rule to only one endpoint" pattern was seen for the first time; on recurrence, a rule should be added to the agent definition.

## My own lessons
No repeatable mistake; no lesson added to my agent definition file.
