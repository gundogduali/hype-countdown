# Retro — INT-1 self-correction pass (2026-07-21, forced review)

## What I did
Re-read my own INT-1 report against the task brief line by line before
finishing, specifically looking for: (a) anything the brief explicitly asked
for that I'd only partially covered, and (b) the "layout shift" half of
acceptance point 1, which I hadn't actually tested (only overlap).

## What I found in review
1. **Gap**: the task said "spot check with the ⚽ soccer-ball curated timer
   specifically" for the og-image sanity check, and to confirm it "works
   correctly for both categories on the coexistence page" (i.e. both the
   normal and ended-state render branches). My original pass only checked
   the ⚽ timer's *active*-state render (`ucl-final-2027`) and used a
   *different*, trophy-emoji timer (`world-cup-2026-final`) for the
   ended-state render. That's not actually a ⚽-specific ended-state check.
2. **Gap**: acceptance point 1 explicitly says "without layout shift issues,"
   and I had only verified the final rendered state has no overlap — I never
   actually captured the loading→loaded transition to check for shift.

## What I fixed / verified now
1. Created a fresh custom timer with emoji `⚽` via the real API, then used
   `sqlite3` directly on the scratch DB to set its `target_at` into the past
   (the API's own `target_in_past` validation blocks creating an
   already-expired timer any other way), then fetched
   `GET /api/timers/:slug/og-image.png` for it and visually inspected the
   PNG: ⚽ ended-state render works correctly (party-popper "It's time!"
   layout, correct title), ⚽ itself still renders monochrome — consistent
   with the already-documented, accepted Skia limitation (PM-11), not a new
   bug. This closes the exact ⚽-ended-state gap the brief asked for.
2. Used Playwright with a `page.route()` interceptor to artificially delay
   the `GET /api/timers/gta-6` response by 1.5s, capturing a real screenshot
   of the actual loading skeleton and comparing it to the loaded state.
   Found a genuine (but pre-existing, not new) layout shift: the loading
   skeleton is much shorter than the full loaded content (which, as of
   v2.2, includes ReactionBar + MessagesSection), and the page's
   `justify-center` container recenters, so everything moves up once data
   arrives. Verified via `git show c7c5844:frontend/src/pages/TimerDetail.jsx`
   that this exact skeleton-vs-loaded-content mismatch pattern already
   existed in the v2.1 initial release (same `justify-center` container,
   Countdown-only loading skeleton) — v2.2 only made the shift's magnitude
   bigger by adding more content below the fold, it didn't introduce a new
   class of bug. Reporting this as a low-severity, non-blocking, pre-existing
   observation (not a FAIL on acceptance point 1, and not something a
   combined page uniquely reveals — same root cause pre-dates all 3 v2.2
   features).

## What the orchestrator needs to know
- All 6 INT-1 acceptance points still PASS; nothing above changes the
  overall sign-off.
- One additional low-severity, non-blocking observation for the record:
  `frontend/src/pages/TimerDetail.jsx`'s loading-state branch (~lines
  100-110) doesn't render placeholders for ReactionBar/MessagesSection, so
  the page visibly grows/recenters once the timer loads (worse than in
  v2.1 since there's now more below-the-fold content). Pre-existing pattern,
  amplified by v2.2, not a regression introduced by this batch. No code
  changed (verification only, per my role).
- Killed the additional scratch server instance (PID 44252) started for this
  review; confirmed via `ps`/`lsof` that nothing remains on port 3093.

## Lesson learned
No new repeatable mistake — both gaps were oversights of thoroughness
(reading the brief's exact wording, e.g. "⚽ specifically" and "both
categories," closely enough) rather than a technique error already covered
by an existing lesson. Not adding a new lessons-learned line since this is a
"read more carefully" gap rather than a distinct reusable technique.
