# Retro — code-reviewer — 2026-07-09 (BE-2 review)

## What I reviewed
The BE-2 polish: the +100-year `target_at` cap, `invalid_body`, `TRUST_PROXY`. Suite 26/26 verified; boundary cases empirically tested with an isolated script (scratchpad).

## Findings and what I did
1. **Security (I fixed it)**: `TRUST_PROXY=true` was converted to boolean true = trust the entire XFF chain → a client could bypass the rate limit with a fake XFF prefix (repro at limit=1 with 3× different fake XFF: 201/201/201). `"true"` is now converted to 1 hop; behind a proxy, the last (proxy-appended) entry of the chain is used. Regression assert + api.md note added.
2. **Contract mismatch (I fixed it)**: api.md said a `null` body is `invalid_body`; the real (and tested) behavior is `invalid_json` (express.json strict). The document was aligned with the real behavior.
3. **Test gap (I closed it)**: the exact +100-year boundary moment wasn't tested; asserts added for the cap moment 201 / +1s 400. The cap is 36525 days ≈ calendar +100 years (+1 day margin) — no leap-year bug, documented in a code comment.
4. **Info (not fixed)**: an invalid TRUST_PROXY (`yes`, `banana`) crashes the server on startup — fail-fast, acceptable; note added to api.md.

## Team update
Second occurrence of the "contract table ↔ test mismatch" pattern in backend-developer (19:58: the `category:""` gap; BE-2: the `null` contradiction) → Rule 7 added to the definition file: whenever an error-table row changes, every example input is to be compared one-to-one against the suite asserts.

## Self-review
All findings were reproduced; 26/26 passing after the fixes; spoof resistance additionally verified in the behind-proxy scenario. My first verification script set up the spoof scenario with a single-entry XFF (it didn't distinguish the fix) — I noticed and repeated it with a multi-entry chain.

## My own mistake caught in the self-correction round
I had added a "(missing Content-Type)" example to the api.md `invalid_body` row without verifying it; empirical testing showed the real behavior is `invalid_title` (without a Content-Type the body stays `{}`; apart from arrays, `invalid_body` is practically untriggerable over HTTP). I corrected the row ("Body is JSON but not an object (e.g. an array)") and added a lesson to my own definition file. Final state: 26/26 green.
