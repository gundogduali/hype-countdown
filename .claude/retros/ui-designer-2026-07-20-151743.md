# UI Designer Retro — 2026-07-20 15:17:43 (SC-2: Social Share Card OG image — confirmed BLOCKED)

## Task
Design a new "Share Card — OG Image" frame (1200x630) for Hype's social preview (PRD §9.1 / BOARD SC-2): emoji, title, remaining-time string, Hype watermark, dark-premium tokens reused from Timer Detail, plus long-title and ended-state variants.

## Review of my own work (self-correction pass)
My first pass concluded the task was blocked because the active Pencil document (`pencil-shadcn.pen`) is an unrelated Rheem claims-management design system, not `ugc_design.pen` (Hype). Before finalizing that conclusion, I re-examined my own evidence for gaps, since "environment is broken" is a strong claim I should verify harder before giving up.

**Additional verification performed in this pass:**
I called `snapshot_layout` with `filePath` set to a deliberately **nonexistent** file (`/Users/aligund/Desktop/projects/claude_ugc/nonexistent_file_xyz.pen`). If `filePath` were being honored/opened, this should error (file not found). Instead it returned the exact same Rheem/shadcn tree as every other call. This is conclusive: in this session, every Pencil MCP read tool (`batch_get`, `get_variables`, `snapshot_layout`) silently ignores the `filePath` argument entirely and always serves whichever document the Pencil desktop app currently has open (`pencil-shadcn.pen`). It is not a path-format issue, not a relative-vs-absolute issue, and not specific to `ugc_design.pen` — it reproduces even for a file that doesn't exist on disk.

There is no tool exposed to me that can open/switch the Pencil desktop app's active document (`get_editor_state` doesn't accept a `filePath` at all; there is no "open file" tool in the available tool set).

## What I fixed
Nothing to fix in produced design artifacts — I made **zero canvas edits**, by design, because any edit would have gone into the wrong (Rheem) file, corrupting an unrelated design system with Hype content built from guessed/unverified tokens (violating "reuse Timer Detail's tokens, don't invent new values" and "don't touch/redesign other screens"). Confirming the blocker harder (via the nonexistent-file test) is itself the fix for this pass — it removes any ambiguity that this might have been a fixable mistake on my part (e.g. wrong path syntax) versus a genuine environment/session issue.

## What the orchestrator needs to know
- This is an **environment/session blocker**, not a scope or design problem. SC-2 itself is fully understood and ready to execute the moment the correct document is active: 1200x630 frame named "Share Card — OG Image", base variant (emoji + title + `Xd Yh Zm` remaining-time string matching Timer Detail's countdown format + small "Hype ⏳" watermark corner mark), a long-title variant (e.g. "FIFA Women's World Cup 2027 Opening") proving no overflow, and an ended-state variant showing "🎉 It's time!" per `docs/copy.md`.
- Action needed before re-dispatch: open `/Users/aligund/Desktop/projects/claude_ugc/ugc_design.pen` in the Pencil desktop app so it becomes the active document, then re-verify with a single cheap `get_editor_state` call that Hype frame names (Explore / Timer Detail / Create) and Hype-style variables actually appear before assuming the fix worked — don't trust tool success alone, the tool call itself will "succeed" either way.
- No files under `backend/`, `frontend/`, or the `.pen` file were touched this session.

## Lesson learned
Already added to `.claude/agents/ui-designer.md` in the prior pass this session (2026-07-20 entry: verify active document actually matches the product before designing; `filePath` silently no-ops instead of erroring). This pass's nonexistent-file test is additional confirming evidence for that same lesson — no new/different lesson to add, so I did not touch the agent definition file again to avoid a duplicate entry.
