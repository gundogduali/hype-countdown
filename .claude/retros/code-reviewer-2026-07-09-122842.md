# Retro — code-reviewer — 2026-07-09 (FE-1 review)

## What I reviewed
FE-1: the `frontend/` Vite+React app — serverNow offset logic, contract compliance (`src/api/client.js` ↔ docs/api.md), PRD Section 3/7 states, token/a11y/render quality. Verified end to end with lint + build + backend/frontend up + headless Chromium (playwright-core, scratchpad).

## Findings and what I did
1. **Correctness (I fixed it)**: `Explore.jsx` category filter race condition — on rapid chip switching, a late old response overwrote the new one (repro: the `oyun` (games) response delayed 1.5s, then switching to `spor` (sports) → active chip Sports, grid showing GAMES cards). `reqId` guard added; after the repro the grid correctly shows SPORTS.
2. **UX (I fixed it)**: `useCopyLink` — on back-to-back copies, old timeouts reset the `✓ Kopyalandı` (= "Copied") label too early; old timers are now cleared on a new copy.
3. **Correctness (I fixed it)**: the `TimerDetail` retry closure re-fetched without an `alive` guard (stale setState on unmount/slug change); redirected to the guarded main effect via `retryKey`.
4. **A11y/consistency (I fixed it)**: in Create, the time input got `aria-invalid` but not the error style (the date input did); the style was aligned with aria.
5. Reported (not fixed): if a card/hero timer expires while the screen is open it stays frozen at 0 (no refetch — product decision); no arrow-key navigation in the emoji radiogroup; the "All" empty list shows the "In this category..." text; gmtLabel shows today's offset (for a DST user it can differ from the target date's — TR unaffected).

## Verification
After the fixes: oxlint clean, `npm run build` clean; e2e green — hero ticks, race fixed, create→redirect→counter→reload, copy = exact URL, boundary moment (8s timer) transition to "Time's up!" with no negatives, 404, detail 500→retry→success, inline validations. Console error-free. Servers stopped after the review.

## Team update
None — frontend-developer's first task; no recurring mistake pattern yet. If the race-condition pattern is seen again, a rule will be added to the definition file.

## Self-review / my own mistake
In my retry e2e setup I used a one-shot 500 mock; dev StrictMode's twin fetch consumed the mock on the first (invisible) request and the test timed out for the wrong reason — and this behavior was written in the FE retro I had read. I made the mock flag-based and fixed it; added a lesson to my own definition file.
