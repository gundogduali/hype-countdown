# Task Board — Hype ⏳

> 2026-07-07: Project relaunched as **Hype** (popular countdowns) (PRD v2.0). Previous work is in the archive (below).

## To Do

> v2.2 batch (PRD §9) — decomposed via the `atomic-plan` skill into single-agent, single-file-scope issues. New agents: `share-card-developer`, `content-moderator`. Do not assign a whole feature as one issue; assign rows.

**9.1 Social Share Card**
- [x] SC-2 — design the OG card visual only (1 frame: title/emoji/remaining time/watermark, dark premium) — `ui-designer` — DONE (3 frames: normal/long-title/ended); see Done log DS-3
- [x] SC-3 — image render service (`backend/src/services/ogImage.*`) — `share-card-developer` — DONE (skia-canvas after prototyping 4 libs for real color-emoji support; sharp/resvg/canvas all failed with tofu/monochrome); 2nd self-correction pass found+fixed a real silent-truncation bug (ellipsis wasn't added when a pathologically long title got dropped past 2 lines); 116/116 tests; see Done log
- [x] SC-4 — route `GET /api/timers/:slug/og-image.png` + cache + `docs/api.md` entry — `share-card-developer` — DONE, folded into the same SC-3 dispatch (task naturally covered both); cache key `slug::target_at::title::emoji` + 60s TTL, 116/116 tests; see Done log
- [x] SC-3c — cache-stampede fix (single-flight `inFlight` promise map) + full `og.test.js` coverage — `share-card-developer` — DONE; adversarially verified the failure/rejection path too (bad key not poisoned, concurrent failures still single-flight-join); no bugs found; see Done log
- [x] DP-4 — **DONE and reviewed**: host-independent color-emoji font (`@fontsource/noto-color-emoji` bundled + registered via `FontLibrary.use()`), so no production-only verification step remains at all. Also fixed a real latent bug: `skia-canvas` had never been declared in `package.json` since SC-3 (would've failed `npm ci` on Render). Review found+fixed a genuine test bug (the pixel-color check couldn't ever fail — background glow alone tripped its threshold; replaced with a differential real-emoji-vs-no-glyph render comparison), verified license (OFL-1.1, fine), and proved a clean `npm ci --omit=dev` install matches Render's exact build command. 144/144 tests. 1 process finding (this task crossed into `share-card-developer`'s exclusive file scope — PM lesson recorded) + 1 doc gap (a few emoji codepoints, incl. ⚽ used by real curated timers, render monochrome regardless of font — Skia limitation, documented, not blocking) — `backend-developer` (dev) + `code-reviewer`; see Done log
- [x] SC-5 — **PM correction (2026-07-20)**: reassigned from `frontend-developer` (client-side meta, invisible to non-JS crawlers) to `backend-developer` (server-side HTML templating for `/t/:slug`, since the SPA fallback served the same static `index.html` for every path). — `backend-developer` — DONE. Task's own agent process hit the 600s watchdog stall (an orphaned `node --watch src/server.js` left running), but PM independently re-verified after killing the stray process: 138/138 tests incl. 4 new SC-5-specific ones, correct absolute-URL og/twitter tags, HTML-escaping of a crafted injection title, unknown-slug/other-paths unaffected (byte-identical). New permanent Rule 7/8 added to both backend-developer.md and frontend-developer.md (never run watch-mode commands in the foreground — this stall pattern hit 2 agents same day). See Done log.
- [x] SC-6 — edge cases (long title, expired, unicode/emoji) + meta-tag verification — `qa-engineer` — DONE, **all PRD §9.1 criteria PASSED**; real expired-timer case (world-cup-2026-final), RTL Arabic + emoji rendering, XSS-injection title fully escaped, single-flight stampede guard spot-checked live; see Done log
- [x] SC-7 — review SC-3+SC-4 (done, approved), now SC-5 — `code-reviewer` — DONE, **approved, zero findings**: escaping coverage traced for all 4 interpolated values, path-matching/traversal-safety verified, TRUST_PROXY consistency confirmed, non-timer paths byte-identical; see Done log

**9.2 Hype Reactions — ✅ FEATURE COMPLETE (design→backend→frontend→review→QA, all passed)**
- [ ] RX-2 — reaction bar component design (isolated component) — `ui-designer`
- [x] RX-3 — data model + `POST /api/timers/:slug/react` + per-device(IP) rate limit + `docs/api.md` — `backend-developer` — DONE, in review (2 self-correction passes, race-safety proven single+cross-process, 95/95 tests); see Done log
- [~] RX-4 — reaction bar UI + optimistic update + localStorage guard — `frontend-developer` — IN PROGRESS
- [x] RX-5 — verify server-side rate-limit enforcement, persistence, no double-count — `qa-engineer` — DONE, **all PRD §9.2 criteria PASSED**; independently re-verified the race-condition fix + 413/invalid_json/invalid_body edge cases live (not just re-reading dev's tests); 8 new backend tests + 1 Playwright e2e; see Done log
- [x] RX-6 — review RX-3+RX-4 — `code-reviewer` — DONE, **approved**; found+fixed a real bug (`ReactionBar.jsx`'s shared `timersRef` bucket let one emoji's tap silently cancel another's toast-hide timer); new permanent Rule 8 added to frontend-developer.md (shared-bucket anti-pattern generalized beyond state to refs/timers/caches); see Done log

**9.3 Hype Messages (moderated free text)**
- [x] HM-2 — message input + list display component design — `ui-designer` — DONE (`Hype/MessageInput` `fWv8X`, `Hype/MessageList` `DDSQB`, gallery `fRAix`); flagged new error copy → PM ratified in `docs/copy.md` v2.2 (all 5 HM-3 codes + reaction rate-limit); placement reference id changed `L6RGt`→`aT1rA` (stale-layout rebuild) — HM-5/RX-4 must read live Pencil state, not this old id
- [x] HM-3 — moderation middleware + wordlist config + bypass-attempt unit tests (`backend/src/middleware/moderation.js`) — `content-moderator` — DONE, in review (2 self-correction passes, real false-positive blocklist bug found+fixed+regression-tested, 96/96 tests); see Done log
- [x] HM-3b — narrowed `urlTlds` to `com net org io info link xyz app ly` (dropped 17 ambiguous ones incl. several the PM didn't list: us/uk/ai/gov/edu); reasoning documented in `backend/src/config/moderation.json`'s `_urlTldsReadme` + `docs/api.md` "Accepted trade-offs"; 102/102 tests, 2 self-correction passes incl. adversarial re-probing — `content-moderator` — DONE, folded into HM-7's eventual full review rather than a separate code-reviewer pass now (PM decision: narrow, explicitly-specified config+doc fix, already thoroughly self-tested)
- [x] HM-4 — data model + `POST/GET /api/timers/:slug/message` wired to HM-3's middleware + `docs/api.md` error codes — `backend-developer` — DONE (FK CASCADE delete verified, all 5 moderation codes trigger via the live route, 116/116 tests incl. new invalid_message type-coverage); see Done log
- [x] HM-5 — message input UI + list rendering + per-error-code user feedback — `frontend-developer` — DONE (retry succeeded); found+fixed 2 real bugs (send-button gradient direction wrong vs. design's `rotation:90`; submit button's client-side-validity disable made `invalid_message`'s error path structurally unreachable by any real click — matched `Create.jsx`'s established "disable only while submitting" convention instead); new lesson recorded; see Done log
- [x] HM-6 — end-to-end bypass-attempt QA pass (mirrors HM-3's own tests via the live API) + happy path — `qa-engineer` — DONE, **all PRD §9.3 criteria PASSED**; found+fixed a real pre-existing e2e-suite bug (chained test scripts shared one 20/hr rate-limit budget, crashed on a fresh DB); 2 doc-clarity notes fixed by PM directly in docs/api.md (spam.gg blocklist collision; rate limit also counts rejected submissions); see Done log
- [x] HM-7 — security-focused review of HM-3+HM-4, **expanded to include HM-5** — `code-reviewer` — DONE, **approved**; 1 probable low-severity finding (`message_too_long` structurally unreachable via the shipped UI since `MessageInput`'s `maxLength={80}` exactly mirrors the server cap). **PM decision (2026-07-20)**: intentional, not a bug — documented in `docs/api.md` as API-level defense-in-depth, no code change; see Done log

**Integration**
- [x] INT-1 — once 9.1–9.3 are all Done, one integration pass over `/t/:slug` with share card + reactions + messages together — `qa-engineer` — DONE, **all 6 acceptance points PASSED**, no bugs found (layout coexistence, independent react+message interaction incl. reload persistence, SSR meta tags coexist with client hydration, expired-timer coexistence, full chained regression 144/144+e2e, ⚽ og-image spot check); 1 pre-existing low-severity cosmetic note (loading-skeleton layout shift, dates back to v2.1, moved to Known minor issues below); see Done log

## In Progress
- (empty)

## In Review
- (empty)

## Known minor issues (v1.1 candidates — from the FE review, deliberately deferred)
- If an Explore card/hero timer expires while the screen is open, it stays frozen at 0 (no refetch)
- No arrow-key navigation in the emoji radiogroup
- "All" empty list shows the "In this category..." text
- gmtLabel DST edge case (TR not affected)
- Timer Detail's loading skeleton doesn't reserve space for below-the-fold content, causing a layout shift once data loads — pre-existing since v2.1, more noticeable now that ReactionBar/MessageInput/MessageList add more below-fold content (found by INT-1, 2026-07-21)
- Emoji codepoints 🕷️ and ⚽ render monochrome (not color) in Share Card og-images regardless of font/host — a `skia-canvas`/Skia glyph limitation, not exhaustively enumerated beyond these two (found during DP-4, 2026-07-20/21)

## Done
- [PM-1] PRD v2.0 written (docs/PRD.md), scope decisions settled with the user
- [PM-2] PRD v2.1 (English), CLAUDE.md (English), docs/copy.md (copy deck) written; FE-3 deviations recorded in the deck (2026-07-09)
- [BE-1] backend API v2: 3 endpoints + seed + rate limit; code-review APPROVED (1 fix, 23/23 tests)
- [DS-1] 7 screens/variants dark premium design — user APPROVED (2026-07-07)
- [BE-2] polish + code-review: TRUST_PROXY rate-limit bypass vulnerability closed by the reviewer, contract aligned; 26/26 tests (2026-07-09)
- [QA-0] PM: seed dates verified from the web; Eurovision + Zelda corrected, iPhone 18 "(expected)" — PRD Appendix A up to date (2026-07-09)
- [BE-3] seed alignment: 3 records updated, seed↔Appendix A 17/17 exact match, 26/26 tests; separate code-review skipped since data-only (PM decision) (2026-07-09)
- [FE-1] frontend + code-review APPROVED: reviewer made 4 fixes (category race, retry guard, copy label, aria-invalid style) (2026-07-09)
- [QA-1] end-to-end verification: **READY TO SHIP, 10/10 criteria PASSED** (38 API + 33 browser checks; report: docs/qa-report.md; persistent smoke: frontend/e2e/smoke.mjs) (2026-07-09)
- [BE-4] QA B-1 closed: >100KB body → 413 `payload_too_large`; the Rule 7 sweep also closed the `invalid_emoji` test gap; 27/27 tests (2026-07-09)
- [FE-2] QA B-2 closed: inline data-URI favicon replaced with a link to the branded favicon.svg; build + console clean (2026-07-09)
- [BE-5] backend fully English: category values + 17 English seeds (slugs included), legacy-DB migration, old-slug cleanup; api.md v2.1; 29/29 tests (2026-07-09)
- [FE-3] frontend fully English: exact match to copy.md, locale fix, smoke 19/19 (English asserts); copy.md deviations recorded in the deck with PM approval (2026-07-09)
- [DS-2] .pen fully English (copy.md v2.1 exact, mock content Appendix A); old frames were already gone; Empty Category id changed Tdh23→CVv0i (2026-07-09)
- [REV-3] translation diff review: **ship-ready** — contract sync, 39 live copy asserts, extra migration idempotency tests; 1 direct fix; 2 nits to FE-4/BE-6, qa-report decision to QA-2 (2026-07-09)
- [FE-4] arrow removed + rate-limit message pluralization; lint+build clean (2026-07-09)
- [BE-6] 429 message plural-aware + new unit test; 31/31 tests (2026-07-09)
- [FE-5] Countdown aria-live plural fix (plural helper); lint+build clean (2026-07-09)
- [QA-2] v2.1 English verification: **READY TO SHIP, 10/10 PASSED** (31/31 backend, 19/19 smoke, ~70 extra checks; migration + clean-DB 17/17; zero Turkish); qa-report.md regenerated in English; 1 new low finding (F-1 → FE-6) + copy.md documentation note (PM handled) (2026-07-10)
- [PM-3] copy.md updated: mobile nav `+ Create`, toast, create hint/footnote, full server-error table (from the QA-2 note) (2026-07-10)
- [TR-2] .claude/ fully English: 6 agent definitions, 24 retro files, BOARD, hook (bash -n OK), settings statusMessage; PM sweep verified — only glossed historical literals remain; orchestrator language rule re-added in English (PM decision) (2026-07-10)
- [FE-6] QA-2 F-1 closed: smoke.mjs regex singular/plural-aware; smoke 19/19 on isolated instance; one-line test-infra change, PM reviewed the diff directly (BE-3 precedent) (2026-07-10)
- [GH-1] repo prep: git init (main) + root .gitignore (dev DB/node_modules/dist ignored, verified) + secret/personal-data sweep clean + demo assets in docs/assets/ (3 desktop PNGs @2x, mobile PNG, 343 KB hero.gif with NumberFlow animation) (2026-07-10)
- [PM-4] README.md (English, hero GIF + screenshots + agentic-team story) + MIT LICENSE written by PM (2026-07-10)
- [GH-2] **Published**: https://github.com/gundogduali/hype-countdown (public, main, 102 files, c7c5844); repo/README/assets verified via gh api; topics added; repo-local git email set to GitHub noreply for privacy (2026-07-10)
- [DP-1] Render deploy prep: Express serves frontend dist when STATIC_DIR set (SPA fallback, cache headers), render.yaml blueprint (free, frankfurt, TRUST_PROXY=true, health check /api/timers); 8 new tests, 39/39; prod-mode boot verified with curl incl. XFF rate-limit keying (2026-07-10)
- [DP-2] DP-1 code review: **ship-ready** — API never falls through to SPA, traversal-safe, render.yaml runtime shape replicated locally; 2 non-blocking notes (stale-hash /assets miss → index.html; app.get('*') is Express-4-only); Rule 7 sharpened in backend-developer.md (3rd assertion-from-memory recurrence); PM: README Live Demo section + Deploy-to-Render button (2026-07-10)
- [DP-1b] First Render deploy FAILED at build: NODE_ENV=production applies at build time → npm ci skipped devDeps → @vitejs/plugin-react missing. Fix: `npm ci --include=dev` in buildCommand; root cause reproduced locally, full envVar build-time audit clean, prod-shape boot re-verified, 39/39; lesson added to backend-developer.md; one-line diff PM-reviewed (2026-07-10)
- [DP-3] **LIVE**: https://hype-countdown.onrender.com — second deploy attempt still used the stale dashboard buildCommand (service settings don't track render.yaml without the GitHub app); user updated the Build Command in the dashboard + manual deploy → live. PM verified end to end: index no-cache, hashed asset immutable 1y, 17 seeds, SPA fallback /t/gta-6, JSON 404 timer_not_found, POST validation English 400 (2026-07-10)
- [PM-5] v2.2 planning: user approved 3-feature scope (Share Card, Reactions, Hype Messages) + a new `/atomic-plan` skill; wrote the skill (`.claude/skills/atomic-plan/SKILL.md`), added 2 new specialist agents (`share-card-developer`, `content-moderator`) with narrow file-scope rules, extended the SubagentStop hook allowlist to cover them (was silently skipping unlisted agent names), updated orchestrator.md Team table, wrote PRD §9 (acceptance criteria for 9.1–9.3), decomposed into 20 atomic To Do issues below (2026-07-20)
- [DS-3] SC-2 share-card design: 3 frames (normal/long-title/ended) reusing existing `$hype-*` tokens and the real `Hype/CountUnit` component; hit and fixed the known stale-top-level-frame render bug via copy/verify/delete-original recipe; screenshots verified against PRD Appendix A + copy.md (2026-07-20)
- [BE-7] RX-3 Hype Reactions backend: fixed 5-emoji set, DB-level (PRIMARY KEY) per-(slug,emoji,ip) uniqueness — proven race-safe under both single-process concurrent and simulated cross-process writes, not just asserted; docs/api.md extended additively; 95/95 tests (2026-07-20)
- [CM-1] HM-3 moderation middleware (content-moderator's first task): length/blocklist/spam-pattern/sanitize checks + bypass-attempt tests; found & fixed a real false-positive (multi-word blocklist phrases matched by substring instead of `\b`-anchored, e.g. would've blocked "meet" for containing "me"); 96/96 tests (2026-07-20)
- [REV-4] Joint review of RX-3+HM-3: **both approved**, no unfixed bugs; independently reproduced the race-safety and blocklist-fix claims rather than trusting the retros; fixed a small docs/api.md changelog gap directly; surfaced 2 product-judgment findings routed to PM (below) rather than fixed unilaterally (2026-07-20)
- [PM-6] Resolved REV-4's 2 findings: (1) ratified permanent per-IP reaction dedup as the real behavior, fixed PRD §9.2's underspecified "rate-limit window" wording to match; (2) ordered a precision-over-recall fix for `checkBareUrl`'s TLD-vs-common-word false positives — dispatched as issue HM-3b (2026-07-20)
- [DS-4] RX-2 `Hype/ReactionBar`+`Hype/ReactionButton` design: default/active states for all 5 emoji, 375px mobile-compact variant, placement reference copy of Timer Detail (original left untouched); discovered `Copy()` on a `reusable` node yields a `ref` not a duplicate — new recipe (rebuild fresh + verify via instance) added to ui-designer.md (2026-07-20)
- [CM-2] HM-3b: narrowed `checkBareUrl`'s TLD allowlist to `com net org io info link xyz app ly` (dropped 17 ambiguous ones, incl. 5 the PM hadn't listed); 102/102 tests, reasoning centralized in `moderation.json`'s `_urlTldsReadme` + `docs/api.md` (2026-07-20)
- [DS-5] HM-2 `Hype/MessageInput`+`Hype/MessageList` design: default/near-limit/error states, empty state matching Explore's tone, mobile variant; correctly flagged new moderation-error copy for PM sign-off instead of shipping it silently (2026-07-20)
- [PM-7] Ratified HM-2's flagged copy: `docs/copy.md` → v2.2, added Hype Reactions & Messages section with all 5 HM-3 error-code messages + reaction/message rate-limit text (2026-07-20)
- [BE-8] HM-4 Hype Messages backend: data model (FK `ON DELETE CASCADE`, verified), route wired to HM-3's middleware (all 5 rejection codes verified live, not just unit-tested), rate limit reusing the existing per-IP service; closed a Rule-7 gap (added non-string `invalid_message` type coverage); 116/116 tests (2026-07-20)
- [SCD-1] SC-3+SC-4 (`share-card-developer`'s first task): prototyped 4 image-rendering libs before picking `skia-canvas` for real color-emoji support (sharp/resvg/canvas all failed with tofu/monochrome glyphs); cache keyed on `slug::target_at::title::emoji` + 60s freshness TTL; 2nd self-correction pass found+fixed a silent-truncation bug (missing "…" on a forcibly-dropped long title); flagged an unverified production risk (color-emoji font may be missing on the Render host) → tracked as DP-4; 116/116 tests (2026-07-20)
- [FE-8] RX-4 reactions frontend: `ReactionBar.jsx` wired into Timer Detail, optimistic updates + localStorage "already reacted" state; 2nd self-correction pass found+fixed a real race (two different emoji tapped before either request settled could clobber each other's state) via per-emoji functional state updates, verified with a scripted Playwright repro; lint+build clean (2026-07-20)
- [REV-5] Joint review of SC-3/SC-4 + HM-4: **both approved**, no bugs in shipped code, concurrent `app.js`/`docs/api.md` edits from 2 agents confirmed to coexist cleanly (again); 2 non-blocking findings surfaced (og-image cache stampede, zero test coverage on og-image) rather than fixed unilaterally, since scope/priority is a PM call (2026-07-20)
- [PM-8] Resolved REV-5's 2 findings: ordered both fixed together as issue SC-3c (test coverage is a required fix per this project's established bar; the cache-stampede fix is small enough to bundle in since the new tests need to exercise concurrent-request behavior anyway) (2026-07-20)
- [SCD-2] SC-3c: single-flight `inFlight` promise map closes the cache-stampede finding + full `og.test.js` coverage added; adversarially verified the rejection/failure path too (bad key not poisoned, concurrent failures still join); 134/134 tests, no bugs found (2026-07-20)
- [QA-3] RX-5: **Hype Reactions — READY TO SHIP**, all PRD §9.2 criteria independently verified PASSED (not re-trusting dev retros); found one gap in its own first pass (had only re-read the dev's 413/invalid_json tests instead of re-running them) and closed it live; 8 new backend tests + 1 Playwright e2e; 2 new QA lessons (Shadow DOM `textContent` trap, `pkill` missing env-prefixed processes) (2026-07-20)
- [MILESTONE] **9.2 Hype Reactions is fully feature-complete**: design (DS-4) → backend (BE-7) → frontend (FE-8) → review (REV-4 partial + RX-6 full, both approved) → QA (QA-3, all PASSED). First of the 3 v2.2 features to close end-to-end (2026-07-20)
- [BE-9] SC-5: server-side `og:*`/`twitter:card` HTML templating for `GET /t/:slug` (`htmlTemplate.js`), reusing `TimerService`; escapes all interpolated values; 138/138 tests. Own process hit a 600s watchdog stall (orphaned `node --watch`), independently re-verified by PM after cleanup; new permanent Rule (7/8) added to both dev agents re: never running watch-mode commands in the foreground (2026-07-20)
- [FE-9] HM-5 (after 1 stalled attempt + 1 successful retry): `MessageInput`/`MessageList` wired below the reaction bar; retry found+fixed 2 real bugs (send-button gradient direction, a client-side disabled-state condition that made `invalid_message`'s error path unreachable); build+lint clean (2026-07-20)
- [REV-6] Joint review of SC-5 + HM-5: **both approved**; SC-5 zero findings (escaping, path-safety, TRUST_PROXY, byte-identical non-timer paths all independently verified); HM-5 1 probable low-severity finding (`message_too_long` unreachable via the shipped UI's `maxLength={80}`); reviewer caught its own near-miss (almost killed another agent's process on the same scratch port, verified ownership via `ps eww`/`lsof` first — new lesson recorded) (2026-07-20)
- [PM-9] Resolved REV-6's finding: ratified the UI's hard `maxLength={80}` cap as intentional (matches the character counter, good UX), `message_too_long` stays as API-level defense-in-depth for non-UI clients — documented in `docs/api.md`, no code change (2026-07-20)
- [QA-4] SC-6+HM-6: **both READY TO SHIP**, all PRD §9.1/§9.3 criteria independently PASSED (real expired-timer + RTL/emoji edge cases, live XSS-injection check, single-flight stampede spot-check, 10+ moderation-bypass attempts incl. leetspeak/zero-width-space/fullwidth-unicode evasion, real 429/Retry-After through the live UI); found+fixed a real pre-existing e2e-suite bug (chained scripts exhausting one shared rate-limit budget); 2 non-blocking doc-clarity findings routed to PM (2026-07-20)
- [PM-10] Resolved QA-4's 2 doc findings directly in `docs/api.md`: swapped the misleading `"spam.gg"` HM-3b trade-off example (independently blocklisted) for a clean one (`"check game.gg later"`), and clarified that the message rate limiter also counts moderation-rejected submissions (intentional, not a bug) (2026-07-20)
- [MILESTONE] **9.1 Social Share Card and 9.3 Hype Messages are both feature-complete** (design → backend → frontend → review → QA, all passed). Only remaining before the full v2.2 batch ships: DP-4 (verify color-emoji font on the real Render deploy host) and INT-1 (final cross-feature integration pass) (2026-07-20)
- [BE-10] DP-4: bundled `@fontsource/noto-color-emoji` + explicit `FontLibrary.use()` registration in `ogImage.js` — emoji rendering now host-independent, no Render-specific verification needed; also fixed `skia-canvas` never being declared in `package.json` since SC-3 (would've broken production `npm ci`). Agent's process was cut off by an API connection error before its own retro/closing pass; PM independently verified code+tests+lockfile and finished one incomplete doc section (2026-07-20/21)
- [REV-7] DP-4 review: **approved**, but found+fixed a real bug in the agent's own new pixel-color test (threshold check could never fail — the background glow alone always tripped it; replaced with a differential real-emoji-vs-no-glyph comparison); verified license (OFL-1.1), proved a clean `npm ci --omit=dev` matches Render's exact build command; 144/144 tests. Recurred its own "write retro before the hook gives the exact path" mistake — this time promoted the lesson into the actual enforceable Rule text (Closing Protocol step 3), not just another Lessons Learned bullet, since the bullet alone hadn't stuck. 2 findings routed to PM (2026-07-21)
- [PM-11] Resolved REV-7's 2 findings: documented the ⚽/🕷️ monochrome-glyph Skia limitation in `docs/api.md` (real user-visible impact — ⚽ is used by real curated timers); recorded a delegation lesson in orchestrator.md about assigning cross-scope follow-up fixes to the actual file owner, or naming the exception explicitly, rather than defaulting to whichever agent owns the task's framing (2026-07-21)
- [MILESTONE] **DP-4 closed — all of v2.2 (9.1, 9.2, 9.3) is now fully done and reviewed.** Only INT-1 (final cross-feature integration pass) remains before reporting ship-readiness to the user (2026-07-21)
- [QA-5] INT-1: **all 6 cross-feature acceptance points PASSED**, zero bugs (layout coexistence desktop+mobile, combined react+message interaction survives reload, SSR og/twitter meta tags coexist with client hydration, expired-timer coexistence, full chained regression 144/144+3 e2e scripts, ⚽ og-image spot check matches documented limitation); caught 2 issues in its own test script before they became false findings (NumberFlow shadow-DOM trap, non-idempotent literal-text assertion) (2026-07-21)
- [MILESTONE] **v2.2 is fully shipped-in-repo and ready**: Social Share Card, Hype Reactions, and Hype Messages — all 3 features complete end-to-end (design → dev → review → QA → integration), 20 atomic issues closed via the new `/atomic-plan` skill, 2 new specialist agents (`share-card-developer`, `content-moderator`) proven out over their first tasks, 144/144 backend tests + 3 passing frontend e2e scripts. Not yet deployed to the live Render URL — that's a user action (dashboard build/deploy), same as DP-1b/DP-3 (2026-07-21)

## Archive (old project — Focus Garden / UGC, cancelled)
- Done: PRD v1.0 + API contract, backend API (19/19 tests), code-review fixes, 6-screen Pencil design
- Not started: frontend, QA
