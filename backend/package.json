{
  "name": "hype-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hype API — countdowns to popular events. Express + SQLite (node:sqlite)",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  }
}
